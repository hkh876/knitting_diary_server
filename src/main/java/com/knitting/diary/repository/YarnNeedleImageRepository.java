package com.knitting.diary.repository;

import com.knitting.diary.domain.YarnNeedleImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface YarnNeedleImageRepository extends JpaRepository<YarnNeedleImage, Long> {
}
